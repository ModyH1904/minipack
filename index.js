let owa = 'your mom';
console.log(owa);

let person = {
    name: 'Mody',
    age: 22
};

function square(number){
    return number * number;
};

aura.innerText = "Mangoes to Mars";

let today = new Date();

let date = today.getFullYear() + ' ' + (today.getMonth()+1)+' '+ today.getDate();

dateToday.innerText = date; 

let text = window.prompt("Enter Text");
p1.innerText = text;



var ooga = 33;

console.log(square(2));
